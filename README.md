# sciona-atoms-signal

Signal-processing provider repo for `sciona.atoms.*` and `sciona.probes.*` namespace packages.

This first migration wave owns:
- `sciona.atoms.signal_processing.biosppy.ecg`
- `sciona.probes.signal_processing.biosppy_ecg`
- `data/heuristics/families/signal_event_rate.json`
- `data/hyperparams/manifest.json`

Compatibility copies remain in `../sciona-atoms` until matcher wiring is fully narrowed.
