{
  "cdg_id": "image_histogram_equalization",
  "name": "image_histogram_equalization",
  "priority": "medium",
  "decomposition_rationale": "Decompose CLAHE into grid-wise contrast-clipped CDF calculation and subsequent spatial bilinear interpolation. This allows substituting alternative local enhancement mapping equations.",
  "family_directory_hint": "src/sciona/atoms/image/filters/",
  "atomic_nodes": [
    {
      "name": "compute_tile_cdfs",
      "semantic_role": "Computes local, contrast-limited cumulative distribution functions in a grid.",
      "source_targets": [
        "skimage.exposure.equalize_adapthist"
      ],
      "inputs": [
        {
          "name": "image",
          "type_desc": "NDArray[np.float64]",
          "required": true
        },
        {
          "name": "kernel_size",
          "type_desc": "tuple[int, int]",
          "required": true
        },
        {
          "name": "clip_limit",
          "type_desc": "float",
          "required": true
        }
      ],
      "outputs": [
        {
          "name": "result",
          "type_desc": "NDArray[np.float64]"
        }
      ],
      "precondition_candidates": [
        "image.ndim == 2",
        "clip_limit >= 0"
      ],
      "postcondition_candidates": [
        "result.ndim == 3"
      ],
      "witness_shape_semantics": "Generates an array of CDF tables matching the number of grid tiles.",
      "time_complexity": "O(w * h)",
      "space_complexity": "O(n_tiles * n_bins)",
      "complexity_reasoning": "Single pass over pixels to build and clip histograms.",
      "complexity_confidence": 94
    }
  ],
  "edges": [],
  "cdg_io": {
    "inputs": [
      {
        "name": "image",
        "type_desc": "NDArray[np.float64]",
        "required": true
      },
      {
        "name": "clip_limit",
        "type_desc": "float",
        "required": true
      }
    ],
    "outputs": [
      {
        "name": "enhanced_image",
        "type_desc": "NDArray[np.float64]"
      }
    ]
  },
  "state_model_needed": false
}